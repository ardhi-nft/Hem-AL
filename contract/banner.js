import chalk from 'chalk';

const printBanner = () => {
    console.log(chalk.cyan('=============================================='));
    console.log(chalk.cyan('           HEMI BOT             '));
    console.log(chalk.cyan('=============================================='));
    console.log(chalk.cyan('Credit By        : Majelis Crypto               '));
    console.log(chalk.cyan('=============================================='));
};

export default printBanner;
